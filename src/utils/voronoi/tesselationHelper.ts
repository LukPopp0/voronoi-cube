export class RadiusCalc {
  /** Scales a plane displacement prior to use in the plane cutting algorithm. */
  static rScale(initialDisplacement: number, ijk: number, q: number) {
    return initialDisplacement;
  }
}
